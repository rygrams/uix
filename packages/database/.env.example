# Connexion directe à PostgreSQL (migrations, Prisma CLI)
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"

# Optionnel : connexion poolée (PgBouncer). Utilisée en priorité par le client applicatif.
# PGBOUNCER_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:6432/${POSTGRES_DB}?schema=public&pgbouncer=true"
